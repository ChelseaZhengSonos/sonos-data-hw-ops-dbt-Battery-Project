---

yaml-files:
  - '*.yaml'
  - '*.yml'
  - .yamllint

extends: default

# sonos overrides
rules:
  line-length:
    max: 120
  quoted-strings:
    quote-type: single
    required: only-when-needed
    allow-quoted-quotes: true

ignore-from-file: [.gitignore, .yamlignore]
